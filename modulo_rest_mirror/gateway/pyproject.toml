[project]
name = "modulo-rest-mirror-gateway"
version = "0.1.0"
description = "Gateway REST/JSON espelho (Modulo P') - fala HTTP com os servidores A' e B'"
requires-python = ">=3.12"

dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "httpx>=0.27.0",
    "python-multipart>=0.0.9",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["main"]
